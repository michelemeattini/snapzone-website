diff --git a/styles.css b/styles.css
--- a/styles.css
+++ b/styles.css
@@ -3198,3 +3198,3 @@
     opacity: var(--slide-opacity, 0);
     transform: translateX(var(--slide-tx, 100vw));
-    transition: opacity 0.35s cubic-bezier(0.16, 1, 0.3, 1), transform 0.35s cubic-bezier(0.16, 1, 0.3, 1);
+    transition: none;
   }
 
   /* Prevent mouse interactions on hidden slides */
@@ -3217,3 +3217,3 @@
     transform: translateX(var(--content-tx, 0));
-    transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1);
+    transition: max-width 0.35s cubic-bezier(0.16, 1, 0.3, 1);
   }
diff --git a/script.js b/script.js
--- a/script.js
+++ b/script.js
@@ -898,2 +898,17 @@
       progress = Math.max(0, Math.min(9, progress));
 
+      // Helper function for smooth non-overlapping opacity curves
+      function getSmoothOpacity(t, isFadeOut) {
+        if (isFadeOut) {
+          // Fade out completely by t = 0.4
+          const k = Math.max(0, 1 - t / 0.4);
+          return k * k * (3 - 2 * k);
+        } else {
+          // Fade in only starting at t = 0.6
+          const k = Math.max(0, Math.min(1, (t - 0.6) / 0.4));
+          return k * k * (3 - 2 * k);
+        }
+      }
+
+      function getSmoothstep(t) {
+        const k = Math.max(0, Math.min(1, t));
+        return k * k * (3 - 2 * k);
+      }
+
       const configs = {};
@@ -908,3 +923,3 @@
       } else if (progress < 3) {
-        configs.hero = { slideTx: -100 * (progress - 2), contentTx: -22, opacity: 1 - (progress - 2) };
+        configs.hero = { slideTx: -100 * (progress - 2), contentTx: -22, opacity: getSmoothOpacity(progress - 2, true) };
       } else {
@@ -917,5 +932,5 @@
       } else if (progress < 2) {
-        configs.gestures = { slideTx: 100 - 100 * (progress - 1), contentTx: 22, opacity: progress - 1 };
+        configs.gestures = { slideTx: 100 - 100 * (progress - 1), contentTx: 22, opacity: getSmoothstep(progress - 1) };
       } else if (progress < 3) {
-        configs.gestures = { slideTx: -100 * (progress - 2), contentTx: 22, opacity: 1 - (progress - 2) };
+        configs.gestures = { slideTx: -100 * (progress - 2), contentTx: 22, opacity: getSmoothOpacity(progress - 2, true) };
       } else {
@@ -928,3 +943,3 @@
       } else if (progress < 3) {
-        configs.fancyzone = { slideTx: 100 - 100 * (progress - 2), contentTx: 0, opacity: progress - 2 };
+        configs.fancyzone = { slideTx: 100 - 100 * (progress - 2), contentTx: 0, opacity: getSmoothOpacity(progress - 2, false) };
       } else if (progress < 4) {
@@ -934,3 +949,3 @@
       } else if (progress < 6) {
-        configs.fancyzone = { slideTx: -100 * (progress - 5), contentTx: -22, opacity: 1 - (progress - 5) };
+        configs.fancyzone = { slideTx: -100 * (progress - 5), contentTx: -22, opacity: getSmoothOpacity(progress - 5, true) };
       } else {
@@ -943,5 +958,5 @@
       } else if (progress < 5) {
-        configs.howItWorks = { slideTx: 100 - 100 * (progress - 4), contentTx: 22, opacity: progress - 4 };
+        configs.howItWorks = { slideTx: 100 - 100 * (progress - 4), contentTx: 22, opacity: getSmoothstep(progress - 4) };
       } else if (progress < 6) {
-        configs.howItWorks = { slideTx: -100 * (progress - 5), contentTx: 22, opacity: 1 - (progress - 5) };
+        configs.howItWorks = { slideTx: -100 * (progress - 5), contentTx: 22, opacity: getSmoothOpacity(progress - 5, true) };
       } else {
@@ -954,3 +969,3 @@
       } else if (progress < 6) {
-        configs.analytics = { slideTx: 100 - 100 * (progress - 5), contentTx: 0, opacity: progress - 5 };
+        configs.analytics = { slideTx: 100 - 100 * (progress - 5), contentTx: 0, opacity: getSmoothOpacity(progress - 5, false) };
       } else if (progress < 7) {
@@ -960,3 +975,3 @@
       } else if (progress < 9) {
-        configs.analytics = { slideTx: -100 * (progress - 8), contentTx: -22, opacity: 1 - (progress - 8) };
+        configs.analytics = { slideTx: -100 * (progress - 8), contentTx: -22, opacity: getSmoothOpacity(progress - 8, true) };
       } else {
@@ -969,5 +984,5 @@
       } else if (progress < 8) {
-        configs.pricing = { slideTx: 100 - 100 * (progress - 7), contentTx: 22, opacity: progress - 7 };
+        configs.pricing = { slideTx: 100 - 100 * (progress - 7), contentTx: 22, opacity: getSmoothstep(progress - 7) };
       } else if (progress < 9) {
-        configs.pricing = { slideTx: -100 * (progress - 8), contentTx: 22, opacity: 1 - (progress - 8) };
+        configs.pricing = { slideTx: -100 * (progress - 8), contentTx: 22, opacity: getSmoothOpacity(progress - 8, true) };
       } else {
@@ -980,3 +995,3 @@
       } else if (progress < 9) {
-        configs.download = { slideTx: 100 - 100 * (progress - 8), contentTx: 0, opacity: progress - 8 };
+        configs.download = { slideTx: 100 - 100 * (progress - 8), contentTx: 0, opacity: getSmoothOpacity(progress - 8, false) };
       } else {
@@ -1000,3 +1015,3 @@
 
-          if (conf.contentTx === 0) {
+          if (Math.abs(conf.contentTx) < 11) {
             slide.classList.add('layout-centered');
